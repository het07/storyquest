/** Cookie holding an anonymous guest identifier for not-yet-signed-in users. */
export const GUEST_COOKIE = "sqa_guest";

/** One year, in seconds. */
export const GUEST_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
